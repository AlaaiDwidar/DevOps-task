apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "toy0store.fullname" . }}-test-connection"
  labels:
    {{- include "toy0store.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "toy0store.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
